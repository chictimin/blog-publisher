# DECISIONS — 선택과 이유 (2026-09-10)

Main Quest "사용자와 함께 완성하는 제작 흐름"의 확장 과제 제출물이다. 제공된 카드뉴스 PRD의 흐름을 **마크다운 초안 발행** 이라는 다른 작업으로 갈아끼운 결과이며, 이 문서는 그 과정에서 내린 결정과 탈락한 대안을 기록한다.

`PRD.md`가 무엇을 만들지를, `CONTRACT.md`가 인터페이스 정본을, `API_SPEC.md`가 경계별 약속을 담는다. 이 문서는 **왜 그렇게 정했는지** 만 담는다.

## 0. 카드뉴스 PRD에서 무엇을 가져오고 무엇을 바꿨나

과제가 제시한 5축을 그대로 대응시켰다.

| 축 | 카드뉴스 원본 | 이 프로젝트 | 재사용 / 교체 |
|---|---|---|---|
| 입력 | 'AI 최신소식' 주제어 | 마크다운 초안 전문, 대상 저장소 `owner/repo` | 교체 |
| AI 판단 | 후보 소식 비교, 독자·형식 제안 | 검수(TODO 잔존·깨진 위키링크·frontmatter 누락·미래 date), slug 중복 판단, 포맷 변환 | 교체 |
| 사람 결정 | 소식 1~3개 선택, 스토리보드 승인 | 발행 승인 1회(`diffPreview` 확인 후 승인·거절) | **재사용**(게이트 구조), 지점은 축소 |
| 도구 행동 | 웹 조사, Antigravity 이미지 생성 | `list_repo_posts`(읽기), `create_publish_pr`(쓰기) | 교체 |
| 검수·산출물 | 카드별 PNG + ZIP + 출처 기록 | 대상 저장소의 PR 1건 + 실행 trace | 교체 |

**재사용한 것**: 조사 → 후보 비교 → 사람 선택 → 도구 실행 → 검수의 골격, 되돌리기 어려운 작업 앞의 승인 게이트, 단계별 진행 상황을 화면에서 볼 수 있게 하는 trace.

**버린 것**: 카드 장수(5~8장), 이미지·글자 분리 배치, 부분 카드 수정, ZIP 다운로드. 카드뉴스 형식에만 필요한 조건이라 이 작업에는 대응물이 없다.

## 1. 해결할 불편과 기대 산출물

- **반복해서 겪는 불편**: devlog 초안이 생길 때마다 사람이 손으로 하던 일 — frontmatter 형식 맞추기, slug 중복 확인, 위키링크·이미지 경로 정리, 브랜치 생성·커밋·PR 생성.
- **확인 가능한 결과물**: 대상 저장소에 `content/blog/<slug>.md`를 추가하는 PR 1건. 사람이 GitHub에서 열어 보고 머지하거나 닫을 수 있으므로 "됐는지" 판정이 명확하다.

## 2. 결정 기록

### D1. 실행 형태 — 완전 클라이언트사이드 웹앱

- **결정**: 백엔드 서버 없음. 모델 호출·GitHub REST·에이전트 루프가 전부 브라우저에서 돈다. GitHub Pages 배포.
- **탈락 대안**: (a) Node/Express 백엔드 — 키를 서버 환경변수에 둘 수 있어 보안상 낫지만, 상시 실행 호스팅과 배포 파이프라인이 필요하다. (b) CLI 전용 — 과제가 요구하는 "브라우저에서 선택하고 답하는 흐름"이 성립하지 않는다.
- **이유**: 이 흐름에서 사람의 결정 지점은 승인 1회뿐이고 상태를 서버에 보관할 필요가 없다. 서버를 없애면 배포가 정적 파일 복사로 끝난다.
- **대가**: 키가 브라우저에 노출되고(D4), 새로고침하면 상태가 사라진다(D9). 둘 다 아래에서 따로 다룬다.

### D2. 모델 제공자 — OpenAI 호환 API + 방문자 입력 baseUrl

- **결정**: `openai` 패키지를 쓰고 `baseUrl`도 방문자가 입력한다. 코드에 기본값을 박지 않는다. 모델 목록은 `GET {baseUrl}/models`로 조회하며 하드코딩하지 않는다.
- **탈락 대안**: Anthropic SDK 직접 호출. 초기 구현은 이쪽이었고 커밋 `b982b66`에서 전환했다.
- **이유**: 개발자가 Anthropic 직접 호출 키를 갖고 있지 않아 실제 실행으로 검증할 수 없었다. 실행하지 못하는 경로를 스펙에 남기면 "동작한다"고 쓸 수 없다. OpenAI 호환 인터페이스는 본가와 호환 게이트웨이 양쪽에서 같은 코드로 돌고, 방문자가 자기 제공자를 넣을 수 있다.
- **근거**: 브라우저 직접 호출은 `dangerouslyAllowBrowser: true`가 필수 (openai TypeScript 레퍼런스, 확인 2026-09-10). CORS 프리플라이트 실측으로 `https://api.openai.com/v1`과 `https://opencode.ai/zen/go/v1` 두 곳의 브라우저 직접 호출 허용을 확인했다(2026-09-10).
- **남는 한계**: 제3의 제공자는 CORS를 막을 수 있다. 앱이 통제할 수 없으므로 결함이 아니라 구조적 한계로 분류하고 `CORS_BLOCKED`로 안내한다.

### D3. GitHub 인증 — fine-grained PAT

- **결정**: 방문자가 PAT를 직접 입력한다. 권한은 대상 저장소 1개 한정, Contents write와 Pull requests write 2개만.
- **탈락 대안**: GitHub OAuth App. 사용자 경험은 이쪽이 낫다.
- **이유**: OAuth는 client secret을 보관하고 code를 토큰으로 교환할 주체가 필요한데, D1에서 서버를 없앴으므로 그 주체가 없다. PAT가 유일하게 남는 경로다.
- **완화**: 권한을 저장소 1개·2개 스코프로 최소화하고 발급 경로를 README와 사용법 모달에 적어 방문자가 과한 권한을 주지 않게 했다.

### D4. 키 보관 — 브라우저 탭 메모리만

- **결정**: `localStorage`·`sessionStorage`에 쓰지 않는다. 새로고침하면 사라지고 재입력한다. 그 사실을 화면에 한 줄로 안내한다.
- **탈락 대안**: `sessionStorage` 보관(새로고침 생존). 편의는 낫다.
- **이유**: OWASP Session Management Cheat Sheet가 웹 저장소를 인증정보 보관처로 명시적으로 비권고한다(확인 2026-09-10). 편의 대비 위험이 크다.
- **대가**: 새로고침 시 재입력. 과제 명세가 요구하는 "새로고침 뒤 진행 상태 이어가기"를 이 프로젝트가 만족하지 못하는 직접 원인이며, 미충족으로 기록한다(D9 참조).

### D5. 사람 개입 지점 — 승인 게이트 1곳

- **결정**: `create_publish_pr` 실행 직전 1회. `diffPreview`(평문 변경 요약)를 보여주고 승인·거절을 받는다. 거절은 오류가 아니며 `status: 'completed'`로 끝난다.
- **탈락 대안**: 2게이트(검수 결과 확인 + 발행 승인). 설계 초기안이 이쪽이었고 CONTRACT 확정 시 1게이트로 축소했다.
- **이유**: 과제 명세는 "꼭 필요한 선택만 요청하고 이미 받은 답은 다시 묻지 않는다"를 요구한다. 검수 결과는 사람이 바꿀 결정이 아니라 읽을 정보라서 trace에 노출하는 것으로 충분하다. 결과가 갈리는 지점은 "이 변경으로 외부 저장소에 PR을 남길 것인가" 하나뿐이다.
- **두 번째 안전장치**: PR까지만 만들고 머지하지 않는다. 되돌리기가 GitHub UI 클릭 한 번이다.

### D6. 도구 — 정확히 2개

- **결정**: `list_repo_posts`(읽기), `create_publish_pr`(쓰기)만 모델에 노출한다.
- **탈락 대안**: `fetch_post_format`(대상 저장소 포맷 학습), 이미지 업로드 도구.
- **이유**: 포맷 판단에 필요한 근거는 `list_repo_posts`가 읽어온 기존 파일에서 이미 얻을 수 있어 별도 도구가 중복이다. 이미지 업로드는 에셋 커밋 경로·중복 처리·용량 판단이 따라붙어 MVP 범위를 넘는다.
- **원칙**: 쓰기 도구는 1개뿐이고 그 앞에 승인 게이트가 있다. 도구를 늘리면 게이트를 늘려야 하므로 늘리지 않았다.

### D7. 실패 처리 — 재시도 1회, 자동 롤백 없음

- **결정**: GitHub 네트워크·5xx 오류는 1회만 재시도하고 `GITHUB_REQUEST_FAILED`로 종료한다. 401/403·404는 재시도하지 않고 즉시 종료한다. 발행 중간 실패 시 부분 생성된 브랜치를 자동으로 지우지 않는다.
- **이유**: 401/403·404는 재시도해도 결과가 같은 오류다. 재시도로 해결되는 건 일시적 네트워크 오류뿐이라 1회로 제한했다. 자동 롤백은 "지우는 동작" 자체가 되돌리기 어려운 쓰기라서, 승인 없이 실행하면 D5의 원칙을 깬다.
- **대가**: 사람이 GitHub UI에서 브랜치를 직접 정리해야 한다. 오류 화면에 그 사실을 안내한다.

### D8. 관찰 가능성 — 토큰 수만, 금액 없음

- **결정**: trace에 단계별 `label`·`detail`·소요시간·입출력 토큰을 남기고 화면에 시간순으로 표시한다. 비용(금액)은 기록하지 않는다.
- **이유**: 모델 API가 단가를 반환하지 않는다. 단가 테이블을 코드에 박으면 낡은 숫자를 실측처럼 보여주게 된다.
- **부수 효과**: 토큰만 남긴 덕에 평가에서 "출력 토큰이 거의 같은데 nano가 mini보다 2배 느리다"는 관찰을 잡아낼 수 있었다(README 평가 절).

### D9. 채택하지 않은 것 — 재개(resume)

- **결정**: IndexedDB 등을 이용한 진행 상태 재개를 넣지 않는다.
- **이유**: 진행 상태를 저장하려면 키도 함께 저장해야 실행을 이어갈 수 있는데 D4가 그것을 금지한다. 키만 재입력받고 상태를 복원하는 절충안도 가능하지만, 승인 게이트 1곳짜리 짧은 흐름이라 재시작 비용이 낮아 복잡도가 값을 못 한다.
- **정직한 기록**: 과제 명세의 "새로고침 뒤에도 선택과 진행 상태를 이어갈 수 있는지" 항목은 **미충족** 이다. 원본 카드뉴스 흐름처럼 단계가 7개이고 이미지 생성이 끼는 긴 작업이었다면 이 결정을 뒤집어야 한다.

### D10. 코드 구조 — core / ui 분리

- **결정**: `src/core/`(모델 호출·도구·루프, UI 의존 없음)와 `src/ui/`(화면)를 분리한다.
- **이유**: 같은 루프를 브라우저와 Node 양쪽에서 돌리기 위해서다. 덕분에 평가를 UI 없이 headless(`npm run eval`)로 6회 자동 실행할 수 있었다. 분리하지 않았다면 평가를 브라우저에서 수기로 6번 돌려야 했다.

### D11. 평가 세트 — 기존 발행 이력 재사용

- **결정**: 새 평가 데이터를 만들지 않고 `devlog/draft/archive/`의 발행 완료 draft와 Hugo 저장소 `content/blog/`의 대응 포스트를 입력·정답 쌍으로 쓴다.
- **실측 정정**: 파일명이 일치하는 쌍은 처음 셈한 11개가 아니라 **8개** 였다(2026-09-10). 나머지 3개(`ai-coding-session-capture`, `cuttoon-copilot-hackathon-retrospective`, `obsidian-capture-skill-cost-redesign`)는 Hugo에 같은 slug가 없어 정답 대조가 불가하므로 세트에서 제외했다.
- **이유**: 정답이 이미 존재하는 데이터가 있는데 새로 만들면 정답의 신뢰도가 떨어진다.

### D12. 비밀값 위치 — 프로젝트 밖

- **결정**: `OPENAI_API_KEY`·`OPENAI_BASE_URL`·`GITHUB_TOKEN`은 `~/.config/blog-publisher/env`(권한 600)에 둔다. 프로젝트 `.env`에는 비밀이 아닌 값(`EVAL_OWNER`·`EVAL_REPO`·`EVAL_MODELS`)만 둔다. 커밋 `8c19cd9`.
- **이유**: 프로젝트 안의 파일을 편집하면 그 내용이 AI 코딩 세션의 컨텍스트로 들어간다. `.gitignore`는 커밋을 막을 뿐 컨텍스트 유입을 막지 못한다.
- **검증**: 프로덕션 dist에서 `VITE_DEV_` 문자열 grep 결과 0건(2026-09-10).

### D13. 검수 결과를 삭제가 아니라 보고로 (2026-09-10 추가)

- **결정**: 검수에서 발견한 문제를 조용히 고쳐 없애지 않는다. `prBody` 맨 앞에 `[고침]`/`[못 고침]`을 붙인 목록으로 남기고, 확인하지 못한 것은 "실재 미확인"으로 적는다. 자료가 부족해 본문이 성립하지 않으면 발행 도구를 호출하지 않는다.
- **계기**: 자료 부족 입력 평가에서 에이전트가 TODO 마커를 삭제하고 깨진 위키링크를 평문으로 바꾼 뒤 "검수 결과: 처리했습니다"라는 성공 서술로 승인 게이트에 올렸다. 결함이 있었다는 사실 자체가 결과물에서 사라졌다(README 실패와 개선 절).
- **원인**: 초기 프롬프트가 검수 항목을 전부 "고쳐라"로만 적고 "보고하라"를 넣지 않았으며, 절차에 중단 경로가 없었다. 에이전트는 지시대로 동작했다.
- **대가**: 프롬프트가 길어져 실행당 입력 토큰이 약 950 늘었다. 정직한 보고의 값으로 치른다.
- **미해결**: 중단 조건을 프롬프트로만 걸어서 지켜지지 않는다. 코드 게이트로 옮기는 것이 다음 단계이며, 판정 임계값을 직관이 아니라 실제 초안 분포에서 뽑아야 해서 이번에 넣지 않았다.

## 3. 아직 결정하지 못한 것

- 대상 저장소 레이아웃이 `content/blog` + TOML frontmatter가 아닐 때의 대응. 현재는 slug 검사 경로 기본값을 바꾸는 수동 대응만 있고, 자동 감지는 넣지 않았다.
- 승인 후 `create_publish_pr`의 실제 PR 생성 경로. 평가 실행 전부에서 승인을 거절해 실행되지 않았다(README 동작 확인 상태 참조).
- 발행 중단의 판정 임계값(실질 문단 몇 개 이상, TODO 제거 후 잔존 문장 길이 몇 자 이상). 실제 초안의 분포를 먼저 재야 하며, 직관으로 정하면 정상 초안을 막는 오탐이 된다.
